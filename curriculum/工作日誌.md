## [2026-07-08 09:25] 新增點名系統連結按鈕
- **按鈕新增**：在標題右側新增「📝 點名系統」按鈕，連結至 `https://attendance-system-15e3f.web.app/`。
- **排版優化**：利用 CSS Flexbox 重新規劃 `header` 結構，使其在不同螢幕尺寸下均能自動排列。
- **樣式整合**：整合原有 PWA 主題配色，為點名按鈕新增漸層色背景、微亮邊框及 hover 陰影與色彩漸變動畫。

---

## [2026-07-01] 暑輔國七新生營隊課程設定

- **新生營隊課程指派**：
  - 實作 `setupSummerCamps()`，為 **國七A** 與 **國七B** 班級指派星期三、四、五第 5~7 節同時分三組上課的國七新生營隊課表：
    * **星期三**（第 5~7 節）：3D列印（唐祥程）、匹克球（星媽）、科學魔法營（蔡佳玲）。
    * **星期四**（第 5~7 節）：高爾夫球（江霂歖）、創意美學（賴雅麗）、遊戲設計（邏輯酷）。
    * **星期五**（第 5~7 節）：生命科學（王世宗）、活力球類（江霂歖）、機器人（黃興國）。
- **新增營隊教師與同步**：
  - 自動偵測並將新增的營隊教師（**星媽、賴雅麗、邏輯酷、黃興國**）補入 `SUMMER_DATA.teachers` 教師清單，確保出現在教師查詢選單，並由 `getTeachers()` 自動排在最末端。
  - 重構流程會自動為這些營隊教師的個人課表同步建立國七A與國七B營隊授課時段。

---

## [2026-07-01] 114-2舊學期資料封存與暑輔課表常駐

- **資料封存與註記**：
  - 將 114-2 的舊學期資料（含 `DATA` 原始學期、`BRIDGING_DATA` 新生銜接、`FINAL_EXAM_DATA` 期末考過濾）在 `data.js` 及 `index.html` 中加入了明確的「備份存檔與註記」中文說明。
  - 保留所有舊資料結構，方便日後以備不時之需，但不對使用者展示。
- **預覽按鈕隱藏與課表常駐**：
  - 隱藏了頂部的「預覽原學期課表/預覽暑輔課表」眼睛按鈕（`#previewBtn`），避免使用者切換到舊課表。
  - 頂部日期看板（`#bridgingDates`）設定為常駐顯示「暑期輔導期間」上課日程。
  - 調整 `getActiveData()` 與 `getTaipeiDateParts()`，強制僅回傳並顯示暑輔課表（`SUMMER_DATA`）及其對應的 7 節課節次。

---

## [2026-06-29] 暑輔分組課缺漏教師資料補齊與同步

- **分組課教師資料補齊**：
  - 補齊了國八、國九分組課缺漏的第三位教師，使各年級分組課的教師組合完全對齊：
    * **國八B**：英文分組課補齊 **莊旭惠** 老師；數學分組課補齊 **蔡佳玲** 老師。
    * **國九A、國九B**：英文分組課補齊 **莊旭惠** 老師；數學分組課補齊 **吳宇綸** 老師。
- **全自動同步機制**：
  - 這些缺漏的教師會自動在 `rebuildSummerTeacherSchedule()` 執行前補入 `classSchedule`。
  - 後續重建教師課表時，會自動在各教師（如莊旭惠、蔡佳玲、吳宇綸）的課表中建立對應的暑輔課程與班級。
  - 隨後的 `applyRoomRules()` 亦會自動將這些新加入的分組英文課程指派至 `5F 視聽教室`，維持資料庫的完美一致性。

---

## [2026-06-24] 暑輔教師課表結構與格式修正

- **暑輔教師課表重構**：
  - 修正了暑輔教師課表 (`SUMMER_DATA.teacherSchedule`) 中班級屬性 `classes: []` 為空，以及因 PDF 解析不完整產生的重複科目與多餘課程條目。
  - 實作並自動呼叫 `rebuildSummerTeacherSchedule()`，由結構完整的 `classSchedule` 反向比對並自動重構 `teacherSchedule`。
  - 確保每一節課的 `classes` 欄位皆包含精準的班級陣列（例如 `["高二理組", "高二文組"]`），使暑輔的「教師查詢」展示格式與原學期和新生銜接完全一致。

---

## [2026-06-22] 教師排序優化與教室規則重構

- **教師選單排序優化**：
  - 修改 `getTeachers()`，將所有排課期間的教師查詢下拉選單排序與原始學期保持一致。
  - 所有在暑期新增的教師（如唐祥程、李偉、聯合報等）一律自動排在選單的最末端，符合使用者閱讀習慣。
- **教室指派規則重構**：
  - 實作 `applyRoomRules()` 函式，於資料載入時動態套用並同步更新 `classSchedule` 與 `teacherSchedule` 的教室設定，並自動重構 `roomSchedule` 與 `rooms` 列表：
    1. **Chad** 所有課程指定至 `3F 多功能 e 化教室 (VR Room)`。
    2. **Roja** 所有課程指定至 `4F 分組教室(二)`。
    3. **Gina** 所有課程指定至 `4F 美術教室`。
    4. **李牧 (高二理組)** 課程指定至 `5F 自主學習教室(一)`。
    5. **李牧 (高一)** 課程指定至 `5F 自主學習教室(二)`。
    6. **英文分組課** 自動將其中一組（依莊旭惠、鄒湘平、羅雅苓優先順序）指派至 `5F 視聽教室`。
    7. **高一週四下午 5~7 節 (第 5, 6, 7 節) AI課程** 全部指定至 `6F 電腦教室`。

---

## [2026-06-22] 課表三階段自動切換與過濾更新

- **三階段課表自動分流與過濾**：
  - **現在 ~ 6/25 (銜接課表)**：自動載入 `BRIDGING_DATA`，含新生銜接班級（新七A、新七B、新高一）及原學期班級。
  - **6/26, 6/29, 6/30 (期末考)**：自動載入過濾後的 `FINAL_EXAM_DATA`，僅包含國七A、國七B、國八A、國八B、高一、高二理組、高二文組，排除了已畢業班級與銜接班級。
  - **7/1 ~ 7/28 (暑期輔導)**：自動切換為新學年班級的 `SUMMER_DATA`（包含國九A、國九B、高三理組、高三文組等新學年班級）。
- **動態提示看板與按鈕切換**：
  - 頂部提示看板（`#bridgingDates`）現為常駐，並會根據當前日期區間自動呈現對應的排課模式與上課說明。
  - 預覽按鈕（`#previewBtn`）具備雙向功能：在非暑輔期間預覽暑輔課表，在暑輔期間預覽原學期課表。
  - 修正了時鐘獲取節次 (periods) 時可能因調用 `getActiveData()` 造成的循環遞迴問題。

---

## [2026-06-22] 暑期輔導課表更新 (SUMMER_DATA)

- **資料解析**：使用 pdfplumber 從 `暑輔教師課表0603.pdf` 與 `暑輔班級課表0603.pdf` 解析 11 個班級（國七A~高三文組）、35 位教師的完整課表資料。
- **資料格式**：解析座標佈局（portrait/landscape 兩種格式），成功提取 7 節課 × 5 天的課表格子，共 534 筆課程條目。
- **data.js 更新**：在 `0622暑輔/data.js` 中新增 `const SUMMER_DATA = {...}` 物件（1265 行），包含 35 位教師的 `teacherSchedule` 與 11 個班級的 `classSchedule`。
- **index.html 更新**：
  - 按鈕文字：「👁️ 預覽新生銜接課表」→「👁️ 預覽暑輔課表」
  - 日期提示：更新為「114學年度暑輔 115.07.01(二)~115.07.28(一)」
  - `getActiveData()` 邏輯：改為偵測 7月1日~7月28日自動切換為 `SUMMER_DATA`
  - `togglePreview()` 邏輯：改為引用 `SUMMER_DATA`

---

## [2026-04-16 12:29] PWA 圖示優化與載入動畫更新

- **圖示優化**：生成並導入 `icon-maskable-512.png`，符合行動端遮罩圖示規範（80% 安全區，純色背景）。
- **配置更新**：`manifest.json` 已加入 `"purpose": "maskable"` 並連結新圖示。
- **載入動畫**：在 `index.html` 加入簡約的 CSS 旋轉旋轉圓環載入效果，並移除舊有圖片過場。
- **部署準備**：確認 `theme_color` 與 `manifest.json` 一致。

---
PWA Optimize Project

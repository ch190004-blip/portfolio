## [2026-04-16 12:29] PWA 圖示優化與載入動畫更新
- **圖示優化**：生成並導入 `icon-maskable-512.png`，符合行動端遮罩圖示規範（80% 安全區，純色背景）。
- **配置更新**：`manifest.json` 已加入 `"purpose": "maskable"` 並連結新圖示。
- **載入動畫**：在 `index.html` 加入簡約的 CSS 旋轉旋轉圓環載入效果，並移除舊有圖片過場。
- **部署準備**：確認 `theme_color` 與 `manifest.json` 一致。

---
PWA Optimize Project
